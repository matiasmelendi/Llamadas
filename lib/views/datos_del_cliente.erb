<!DOCTYPE html>
<html>
<head> <title> Datos del cliente </title> </head>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.2.0/css/bootstrap.min.css">

    <body>
        <div class="container">
            <div align="justify"  class="panel panel-success">

                <div class="panel-heading"> Datos de <%= @cliente.nombre %> </div>

                <div class="panel-body" >
                    <ul>
                        <li> Número: <%= @cliente.linea.numero %> </li>
                        <li> Código de area local: <%= @cliente.linea.cod_area.cod_local %> </li>
                        <li> Código de area nacional: <%= @cliente.linea.cod_area.cod_nacional %> </li>
                    </ul>
                    <ul> Llamadas
                        <% @compania.llamadas_del_cliente(@cliente.nombre,@mes_del_anho).each{ |llamada| %>
                            <li> A: <%=llamada.receptor.nombre%> Duración: <%= llamada.duracion_en_minutos.to_s + " minutos " %> Mes del año: <%= llamada.mes.to_s + "/" + llamada.año.to_s  %>  </li>
                        <% } %>
                    </ul>
                    <a class="btn btn-default" role="button" href="/" > Volver a la página principal</a>

                    <a class="btn btn-default" role="button" href=<%="/facturar/?mes=" + @mes_del_anho.mes.to_s + "&nombre=" + @cliente.nombre %> > Facturar </a>
                </div>

            </div>
        </div>
    </body>

</html>